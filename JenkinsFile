pipeline {
    agent any

    tools {
        jdk 'JDK'
        gradle 'Gradle'
    }

    stages {
        stage('Build') {
            steps {
                bat 'gradle clean build'
            }
        }

        stage('Test') {
            steps {
                bat 'gradle test'
            }
        }

        stage('Package') {
            steps {
                bat 'gradle assemble'
            }
        }

        stage('Run') {
            steps {
                bat 'gradle :app:run'
            }
        }
    }

    post {
        success {
            echo 'Build Successful'
        }
        failure {
            echo 'Build Failed'
        }
    }
}
